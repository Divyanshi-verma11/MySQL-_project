-- 14. Find total sales amount for each month.

SELECT 
    MONTHNAME(order_purchase_timestamp) as Months, SUM(price) AS Total_sale
FROM
    project.olist_orders_dataset t1
        JOIN
    olist_order_items_dataset t2 ON t1.order_id = t2.order_id
GROUP BY MONTHNAME(order_purchase_timestamp);
