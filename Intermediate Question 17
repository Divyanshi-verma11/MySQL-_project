-- 17. Find total revenue generated by each product category.

SELECT 
    t1.product_category_name, ROUND(SUM(price), 2) AS revenue
FROM
    olist_products_dataset t1
        JOIN
    olist_order_items_dataset t2 ON t1.product_id = t2.product_id
GROUP BY product_category_name;
