-- 11. Calculate total spending by each customer.

SELECT 
    t2.customer_id, SUM(payment_value) AS total_spending
FROM
    project.olist_order_payments_dataset t1
        JOIN
    olist_orders_dataset t2 ON t1.order_id = t2.order_id
GROUP BY t2.customer_id;
