-- 12. Find customers who never placed any order.

SELECT 
    *
FROM
    project.olist_customers_dataset
WHERE
    customer_id NOT IN (SELECT 
            customer_id
        FROM
            olist_orders_dataset);
